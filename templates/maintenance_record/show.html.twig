{% extends 'base.html.twig' %}

{% block title %}MaintenanceRecord{% endblock %}

{% block body %}
    <h1>MaintenanceRecord</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ maintenance_record.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ maintenance_record.type }}</td>
            </tr>
            <tr>
                <th>OtherType</th>
                <td>{{ maintenance_record.otherType }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ maintenance_record.date ? maintenance_record.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Mileage</th>
                <td>{{ maintenance_record.mileage }}</td>
            </tr>
            <tr>
                <th>Notes</th>
                <td>{{ maintenance_record.notes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_maintenance_record_index') }}">back to list</a>

    <a href="{{ path('app_maintenance_record_edit', {'id': maintenance_record.id}) }}">edit</a>

    {{ include('maintenance_record/_delete_form.html.twig' , {maintenance_record: maintenance_record}) }}
{% endblock %}
